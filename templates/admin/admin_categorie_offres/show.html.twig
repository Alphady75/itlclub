{% extends 'base.html.twig' %}

{% block title %}CategorieOffre{% endblock %}

{% block body %}
    <h1>CategorieOffre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_offre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_offre.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categorie_offre.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_categorie_offres_index') }}">back to list</a>

    <a href="{{ path('admin_categorie_offres_edit', {'id': categorie_offre.id}) }}">edit</a>

    {{ include('admin_categorie_offres/_delete_form.html.twig') }}
{% endblock %}
