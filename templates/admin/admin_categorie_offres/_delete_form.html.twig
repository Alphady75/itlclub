<form method="post" action="{{ path('admin_categorie_offres_delete', {'id': categorie_offre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_offre.id) }}">
    <button class="btn">Delete</button>
</form>
