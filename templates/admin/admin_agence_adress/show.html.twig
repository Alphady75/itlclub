{% extends 'base.html.twig' %}

{% block title %}AgenceAdress{% endblock %}

{% block body %}
    <h1>AgenceAdress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agence_adress.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ agence_adress.number }}</td>
            </tr>
            <tr>
                <th>Street</th>
                <td>{{ agence_adress.street }}</td>
            </tr>
            <tr>
                <th>PostalCode</th>
                <td>{{ agence_adress.postalCode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ agence_adress.city }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ agence_adress.created ? agence_adress.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated</th>
                <td>{{ agence_adress.updated ? agence_adress.updated|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_agence_adress_index') }}">back to list</a>

    <a href="{{ path('admin_agence_adress_edit', {'id': agence_adress.id}) }}">edit</a>

    {{ include('admin_agence_adress/_delete_form.html.twig') }}
{% endblock %}
