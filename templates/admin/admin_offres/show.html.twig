{% extends 'admin.html.twig' %}

{% block title %}Offres{% endblock %}

{% block body %}
    <h1>Offres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ offre.name }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ offre.imageName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre.description }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ offre.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_offres_index') }}">back to list</a>

    <a href="{{ path('admin_offres_edit', {'id': offre.id}) }}">edit</a>

    {{ include('admin/admin_offres/_delete_form.html.twig') }}
{% endblock %}
