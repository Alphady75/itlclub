{% extends 'admin.html.twig' %}

{% block title %}Edition du contrat N°{{ contract.id }} - {{ contract.company.name }}{% endblock %}

{% block body %}

<div class="card shadow-sm">
    <div class="card-header py-3"><h6 class="mb-0 fw-bold">Edition du contrat N°{{ contract.id }} - {{ contract.company.name }}</h6></div>

    <div class="card-body">

        {{ include('admin/admin_contract/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}

        {{ include('admin/admin_contract/_delete_form.html.twig') }}

    </div>
</div>
{% endblock %}
