{{ form_start(form) }}
<div class="row">
    <div class="col-md-8">
        {{ form_widget(form) }}
        
        <button class="btn btn-dark">{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('admin_agency_index') }}" class="btn btn-secondary">Annuler</a>
    </div>
</div>
{{ form_end(form) }}
