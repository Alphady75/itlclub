{% extends 'admin.html.twig' %}

{% block title %}Mettre à jour le formulaire Solipac{% endblock %}

{% block body %}

<div class="card">
    <div class="card-body p-4">

        {{ include('solipac/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="row mt-4">
            <div class="col-sm-9 mx-auto">
                {{ include('admin/admin_solipac/_delete_form.html.twig') }}
            </div>
        </div>

    </div>
</div>

{% endblock %}