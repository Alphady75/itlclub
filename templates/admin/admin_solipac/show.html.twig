{% extends 'admin.html.twig' %}

{% block title %}Formulaire solipac n°{{ solipac.id }} {% endblock %}

{% block body %}

<div class="card">

    <div class="card-body p-4">

        {{ include('admin/admin_solipac/_details.html.twig') }}

        <div class="pt-5">
            <a href="{{ path('app_admin_solipac_edit', {'id': solipac.id}) }}" class="btn btn-secondary mb-3">Mettre à
                jour</a>

            {{ include('admin/admin_solipac/_delete_form.html.twig') }}

            {{ include('admin/admin_solipac/_download.html.twig') }}

        </div>

    </div>

</div>
{% endblock %}