<form method="post" action="{{ path('app_admin_solipac_delete', {'id': solipac.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solipac.id) }}">
    <button class="btn btn-danger text-light"><i class="fa fa-trash-alt"></i> Supprimer</button>
</form>
