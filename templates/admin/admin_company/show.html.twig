{% extends 'admin.html.twig' %}

{% block title %}Company{% endblock %}

{% block body %}
    <h1>Company</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ company.name }}</td>
            </tr>
            <tr>
                <th>Siret</th>
                <td>{{ company.siret }}</td>
            </tr>
            <tr>
                <th>NbEmployees</th>
                <td>{{ company.nbEmployees }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ company.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ company.email }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ company.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_company_index') }}">back to list</a>

    <a href="{{ path('admin_company_edit', {'id': company.id}) }}">edit</a>

    {{ include('admin/admin_company/_delete_form.html.twig') }}
{% endblock %}
