{{ form_start(form) }}
<div class="text-center">
	<legend>DEVENEZ PARTENAIRE</legend>

	<p class="font-weight-light">Vous souhaitez intégrer le club intellia en tant que partenaire ? Pour cela prenez contact avec
		nous en remplissant ce formulaire. Un gestionnaire dédié prendra contact avec vous sous 24h
	pour prendre connaissance de votre demande.</p>
</div>

<div class="row">
	<div class="col-md-6">
		{{ form_row(form.lastname) }}
	</div>
	<div class="col-md-6">
		{{ form_row(form.name) }}
	</div>
</div>
<div class="row">
	<div class="col-md-6">
		{{ form_row(form.telephone) }}
	</div>
	<div class="col-md-6">
		{{ form_row(form.email) }}
	</div>
</div>
<div class="form-group">
	{{ form_row(form.partenaireInteret) }}
</div>

<div class="form-group">
	<p>Je souhaite être recontacté par:</p>
	{{ form_row(form.contactedBy) }}
</div>

<div class="form-group">
	<button type="submit" class="btn primary-btn">{{ button_label|default('Envoyer') }}</button>
</div>
{{ form_end(form) }}
