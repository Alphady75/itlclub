{% extends 'base.html.twig' %}

{% block title %}Votre compte {{ user.lastname }}{% endblock %}

{% block body %}
<section class="user-compte-section costom-padding">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <img src="assets/img/core/user.jpg" width="100" height="100" class="rounded-circle"> <br>
                <strong>{{ app.user.email }}</strong>
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-md-6">
                <a href="compte.html" class="d-block mb-4"><i class="fa fa-user"></i> Votre compte</a>

                <a href="{{ path('app_logout') }}" class="btn primary-btn"><i class="fa fa-power-off"></i> Déconnexion</a>
            </div>
        </div>
    </div>
</section>
{% endblock %}
