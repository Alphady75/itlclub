{% extends 'base.html.twig' %}

{% block title %}Changer votre mot de passe{% endblock %}

{% block body %}
<section class="user-compt-section costom-padding">
	<div class="container">
		<div class="row">
			<div class="col-md-4">

				{% include('user/_sideBare.html.twig') %}
				
			</div>

			<div class="col-md-6">
				<div class="form-input-border label-bold">
					{{ form_start(resetForm) }}
						<legend class="font-weight-bold h5"><i class="fa fa-cog"></i> Modifier le mot de passe</legend>
						{{ form_row(resetForm.plainPassword) }}

						<div class="form-group mt-5 mb-5">
							<button class="btn primary-btn">Mettre à jour le mot de passe</button>
						</div>
					{{ form_end(resetForm) }}
				</div>
			</div>
		</div>
	</div>
</section>	
{% endblock %}